# Environment variables for Azure Linux Web App
# Copy this to .env in your Azure App Service Configuration

# Node environment - set to production for App Service
NODE_ENV=production

# Port (Azure uses port 8080 by default for Linux)
PORT=8080